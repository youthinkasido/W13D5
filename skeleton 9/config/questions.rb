# what is json in the context of a view?
#
#
#at what point does store update?

# MapstatetoProps(state, action) // returns object // where is it called from?

# { receiveTodo:receiveTodo,

#     }

# Calling,binding the same idea as thunk?


# .then() does it take in response of ajax or the promise?

store.dispatch takes in an action, hits all the reducers, all reducers returns a new state?, 
state updated via connect method in component?

#logger intercepts errors, is that the only functionality?

# dispatch(action.type)

action = DEPOSIT

reducer = TELLER

store = VAULT 

state = MONEY

